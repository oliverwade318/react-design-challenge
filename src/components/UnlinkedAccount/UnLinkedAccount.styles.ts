import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  container: {
    marginTop: "4vh",
    marginBottom: "10vh",
  },
  card: { minWidth: 275, maxWidth: 400, borderRadius: 10 },
});
